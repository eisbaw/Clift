-- Generated by CImporter multi_import. Do not edit.
-- Source file: Helper
-- Project: MultiProject

import Clift.CSemantics
import Generated.MultiProject.Types

set_option maxHeartbeats 400000
set_option linter.unusedVariables false

open MultiProject.Types

namespace MultiProject.Helper

/-- Local variables (merged from all functions).
    NOTE: Locals use Inhabited default (zero-init). See task-0060. -/
structure Locals where
  a : Ptr C_point
  b : Ptr C_point
  dx : UInt32
  dy : UInt32
  ret__val : UInt32
  deriving Repr, DecidableEq, Inhabited

/-- Program state combining globals and locals. -/
abbrev ProgramState := CState Locals

/-- CSimpl body for distance_sq(). -/
def distance_sq_body : CSimpl ProgramState :=
  .catch
    (
      .seq
        (
          .guard (fun s => heapPtrValid s.globals.rawHeap s.locals.a)
            (
              .guard (fun s => heapPtrValid s.globals.rawHeap s.locals.b)
                (
                  .basic (fun s => { s with locals := { s.locals with dx := ((hVal s.globals.rawHeap s.locals.a).x - (hVal s.globals.rawHeap s.locals.b).x) } })
                )
            )
        )
        (
          .seq
            (
              .guard (fun s => heapPtrValid s.globals.rawHeap s.locals.a)
                (
                  .guard (fun s => heapPtrValid s.globals.rawHeap s.locals.b)
                    (
                      .basic (fun s => { s with locals := { s.locals with dy := ((hVal s.globals.rawHeap s.locals.a).y - (hVal s.globals.rawHeap s.locals.b).y) } })
                    )
                )
            )
            (
              .seq (.basic (fun s => { s with locals := { s.locals with ret__val := ((s.locals.dx * s.locals.dx) + (s.locals.dy * s.locals.dy)) } })) .throw
            )
        )
    )
    .skip

/-- Procedure environment mapping function names to bodies. -/
def procEnv : ProcEnv ProgramState :=
  ProcEnv.insert (ProcEnv.empty) "distance_sq" distance_sq_body

end MultiProject.Helper

