-- Generated by CImporter from clang JSON AST. Do not edit.
-- Source module: ForLoop
-- Regenerate with: just import test/c_sources/forloop.c ForLoop

import Clift.CSemantics

set_option maxHeartbeats 400000
set_option linter.unusedVariables false

namespace ForLoop

/-- Local variables (merged from all functions).
    NOTE: Locals use Inhabited default (zero-init). See task-0060. -/
structure Locals where
  i : UInt32
  n : UInt32
  ret__val : UInt32
  sum : UInt32
  deriving Repr, DecidableEq, Inhabited

/-- Program state combining globals and locals. -/
abbrev ProgramState := CState Locals

/-- CSimpl body for sum_to_n(). -/
def sum_to_n_body : CSimpl ProgramState :=
  .catch
    (
      .seq
        (
          .basic (fun s => { s with locals := { s.locals with sum := 0 } })
        )
        (
          .seq
            (
              .seq
                (
                  .basic (fun s => { s with locals := { s.locals with i := 0 } })
                )
                (
                  .while (fun s => decide (s.locals.i < s.locals.n))
                    (
                      .seq
                        (
                          .basic (fun s => { s with locals := { s.locals with sum := (s.locals.sum + s.locals.i) } })
                        )
                        (
                          .basic (fun s => { s with locals := { s.locals with i := (s.locals.i + 1) } })
                        )
                    )
                )
            )
            (
              .seq (.basic (fun s => { s with locals := { s.locals with ret__val := s.locals.sum } })) .throw
            )
        )
    )
    .skip

/-- Procedure environment mapping function names to bodies. -/
def procEnv : ProcEnv ProgramState :=
  ProcEnv.insert (ProcEnv.empty) "sum_to_n" sum_to_n_body

end ForLoop
