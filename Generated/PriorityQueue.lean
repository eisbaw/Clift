-- Generated by CImporter from clang JSON AST. Do not edit.
-- Source module: PriorityQueue
-- Regenerate with: just import test/c_sources/priorityqueue.c PriorityQueue

import Clift.CSemantics

set_option maxHeartbeats 400000
set_option linter.unusedVariables false

namespace PriorityQueue

/-- C struct pqueue: size=8, align=4 -/
structure C_pqueue where
  size : UInt32
  capacity : UInt32
  deriving Repr, Inhabited

instance : CType C_pqueue where
  size := 8
  align := 4
  typeTag := ⟨200⟩
  size_pos := by omega

/-- Read C_pqueue from 8 bytes. -/
def C_pqueue.fromBytes (f : Fin 8 → UInt8) : C_pqueue where
  size := UInt32.fromBytes' (fun i => f ⟨0 + i.val, by omega⟩)
  capacity := UInt32.fromBytes' (fun i => f ⟨4 + i.val, by omega⟩)

/-- Write C_pqueue to 8 bytes. -/
def C_pqueue.toBytes (v : C_pqueue) : Fin 8 → UInt8 := fun i =>
  if h : 0 ≤ i.val ∧ i.val < 4 then
    UInt32.toBytes' v.size ⟨i.val - 0, by omega⟩
  else
    if h : 4 ≤ i.val ∧ i.val < 8 then
      UInt32.toBytes' v.capacity ⟨i.val - 4, by omega⟩
    else
      0  -- padding bytes

private theorem C_pqueue.toBytes_size_byte (v : C_pqueue) (i : Nat) (hi : i < 4) :
  C_pqueue.toBytes v ⟨0 + i, by omega⟩ = UInt32.toBytes' v.size ⟨i, hi⟩ := by
    unfold C_pqueue.toBytes
    rw [dif_pos (show (0 ≤ (0 + i) ∧ (0 + i) < 4) from by omega)]
    congr 1; apply Fin.ext; simp

private theorem C_pqueue.toBytes_capacity_byte (v : C_pqueue) (i : Nat) (hi : i < 4) :
  C_pqueue.toBytes v ⟨4 + i, by omega⟩ = UInt32.toBytes' v.capacity ⟨i, hi⟩ := by
    unfold C_pqueue.toBytes
    rw [dif_neg (show ¬(0 ≤ (4 + i) ∧ (4 + i) < 4) from by omega)]
    rw [dif_pos (show (4 ≤ (4 + i) ∧ (4 + i) < 8) from by omega)]
    congr 1; apply Fin.ext; simp

/-- Roundtrip: fromBytes (toBytes v) = v. -/
theorem C_pqueue.fromBytes_toBytes (v : C_pqueue) :
  C_pqueue.fromBytes (C_pqueue.toBytes v) = v := by
    have h_size : (fun i : Fin 4 => C_pqueue.toBytes v ⟨0 + i.val, by omega⟩) = UInt32.toBytes' v.size := by
      funext ⟨i, hi⟩; exact C_pqueue.toBytes_size_byte v i hi
    have h_capacity : (fun i : Fin 4 => C_pqueue.toBytes v ⟨4 + i.val, by omega⟩) = UInt32.toBytes' v.capacity := by
      funext ⟨i, hi⟩; exact C_pqueue.toBytes_capacity_byte v i hi
    show C_pqueue.fromBytes (C_pqueue.toBytes v) = v
    unfold C_pqueue.fromBytes
    rw [h_size, h_capacity, UInt32.fromBytes_toBytes' v.size, UInt32.fromBytes_toBytes' v.capacity]
    try { cases v; rfl }

instance : MemType C_pqueue where
  size := 8
  align := 4
  typeTag := ⟨200⟩
  size_pos := by omega
  fromMem := C_pqueue.fromBytes
  toMem := C_pqueue.toBytes
  roundtrip := C_pqueue.fromBytes_toBytes

/-- Local variables (merged from all functions).
    NOTE: Locals use Inhabited default (zero-init). See task-0060. -/
structure Locals where
  capacity : UInt32
  data : Ptr UInt32
  heap_size : UInt32
  i : UInt32
  iters : UInt32
  j : UInt32
  left : UInt32
  out : Ptr UInt32
  parent : UInt32
  pq : Ptr C_pqueue
  ret__val : UInt32
  right : UInt32
  root_idx : UInt32
  smallest : UInt32
  tmp : UInt32
  value : UInt32
  deriving Repr, DecidableEq, Inhabited

/-- Program state combining globals and locals. -/
abbrev ProgramState := CState Locals

/-- CSimpl body for pq_init(). -/
def pq_init_body : CSimpl ProgramState :=
  .catch
    (
      .seq
        (
          .guard (fun s => heapPtrValid s.globals.rawHeap s.locals.pq)
            (
              .basic (fun s => { s with globals := { s.globals with rawHeap := heapUpdate s.globals.rawHeap s.locals.pq ({ hVal s.globals.rawHeap s.locals.pq with size := 0 }) } })
            )
        )
        (
          .guard (fun s => heapPtrValid s.globals.rawHeap s.locals.pq)
            (
              .basic (fun s => { s with globals := { s.globals with rawHeap := heapUpdate s.globals.rawHeap s.locals.pq ({ hVal s.globals.rawHeap s.locals.pq with capacity := s.locals.capacity }) } })
            )
        )
    )
    .skip

/-- CSimpl body for pq_swap(). -/
def pq_swap_body : CSimpl ProgramState :=
  .catch
    (
      .seq
        (
          .guard (fun s => heapPtrValid s.globals.rawHeap (Ptr.elemOffset s.locals.data s.locals.i.toNat))
            (
              .basic (fun s => { s with locals := { s.locals with tmp := (hVal s.globals.rawHeap (Ptr.elemOffset s.locals.data s.locals.i.toNat)) } })
            )
        )
        (
          .seq
            (
              .guard (fun s => heapPtrValid s.globals.rawHeap (Ptr.elemOffset s.locals.data s.locals.i.toNat))
                (
                  .guard (fun s => heapPtrValid s.globals.rawHeap (Ptr.elemOffset s.locals.data s.locals.j.toNat))
                    (
                      .basic (fun s => { s with globals := { s.globals with rawHeap := heapUpdate s.globals.rawHeap (Ptr.elemOffset s.locals.data s.locals.i.toNat) (hVal s.globals.rawHeap (Ptr.elemOffset s.locals.data s.locals.j.toNat)) } })
                    )
                )
            )
            (
              .guard (fun s => heapPtrValid s.globals.rawHeap (Ptr.elemOffset s.locals.data s.locals.j.toNat))
                (
                  .basic (fun s => { s with globals := { s.globals with rawHeap := heapUpdate s.globals.rawHeap (Ptr.elemOffset s.locals.data s.locals.j.toNat) s.locals.tmp } })
                )
            )
        )
    )
    .skip

/-- CSimpl body for pq_bubble_up(). -/
def pq_bubble_up_body : CSimpl ProgramState :=
  .catch
    (
      .while (fun s => decide (s.locals.i > 0))
        (
          .seq
            (
              .guard (fun s => 2 ≠ 0)
                (
                  .basic (fun s => { s with locals := { s.locals with parent := ((s.locals.i - 1) / 2) } })
                )
            )
            (
              .cond (fun s => decide ((hVal s.globals.rawHeap (Ptr.elemOffset s.locals.data s.locals.i.toNat)) < (hVal s.globals.rawHeap (Ptr.elemOffset s.locals.data s.locals.parent.toNat))))
                (
                  .seq
                    (
                      .dynCom (fun saved =>
                        .seq
                          (.basic (fun s => { s with locals := { s.locals with data := s.locals.data, i := s.locals.i, j := s.locals.parent } }))
                          (.seq
                            (.call "pq_swap")
                            (.basic (fun s => { s with locals := saved.locals }))))
                    )
                    (
                      .basic (fun s => { s with locals := { s.locals with i := s.locals.parent } })
                    )
                )
                (
                  .throw
                )
            )
        )
    )
    .skip

/-- CSimpl body for pq_bubble_down(). -/
def pq_bubble_down_body : CSimpl ProgramState :=
  .catch
    (
      .seq
        (
          .basic (fun s => { s with locals := { s.locals with iters := 0 } })
        )
        (
          .while (fun s => decide (s.locals.iters < s.locals.heap_size))
            (
              .seq
                (
                  .basic (fun s => { s with locals := { s.locals with left := ((2 * s.locals.i) + 1) } })
                )
                (
                  .seq
                    (
                      .basic (fun s => { s with locals := { s.locals with right := ((2 * s.locals.i) + 2) } })
                    )
                    (
                      .seq
                        (
                          .basic (fun s => { s with locals := { s.locals with smallest := s.locals.i } })
                        )
                        (
                          .seq
                            (
                              .cond (fun s => (decide (s.locals.left < s.locals.heap_size) && decide ((hVal s.globals.rawHeap (Ptr.elemOffset s.locals.data s.locals.left.toNat)) < (hVal s.globals.rawHeap (Ptr.elemOffset s.locals.data s.locals.smallest.toNat)))))
                                (
                                  .basic (fun s => { s with locals := { s.locals with smallest := s.locals.left } })
                                )
                                .skip
                            )
                            (
                              .seq
                                (
                                  .cond (fun s => (decide (s.locals.right < s.locals.heap_size) && decide ((hVal s.globals.rawHeap (Ptr.elemOffset s.locals.data s.locals.right.toNat)) < (hVal s.globals.rawHeap (Ptr.elemOffset s.locals.data s.locals.smallest.toNat)))))
                                    (
                                      .basic (fun s => { s with locals := { s.locals with smallest := s.locals.right } })
                                    )
                                    .skip
                                )
                                (
                                  .seq
                                    (
                                      .cond (fun s => decide (s.locals.smallest = s.locals.i))
                                        (
                                          .throw
                                        )
                                        .skip
                                    )
                                    (
                                      .seq
                                        (
                                          .dynCom (fun saved =>
                                            .seq
                                              (.basic (fun s => { s with locals := { s.locals with data := s.locals.data, i := s.locals.i, j := s.locals.smallest } }))
                                              (.seq
                                                (.call "pq_swap")
                                                (.basic (fun s => { s with locals := saved.locals }))))
                                        )
                                        (
                                          .seq
                                            (
                                              .basic (fun s => { s with locals := { s.locals with i := s.locals.smallest } })
                                            )
                                            (
                                              .basic (fun s => { s with locals := { s.locals with iters := (s.locals.iters + 1) } })
                                            )
                                        )
                                    )
                                )
                            )
                        )
                    )
                )
            )
        )
    )
    .skip

/-- CSimpl body for pq_insert(). -/
def pq_insert_body : CSimpl ProgramState :=
  .catch
    (
      .seq
        (
          .cond (fun s => decide ((hVal s.globals.rawHeap s.locals.pq).size >= (hVal s.globals.rawHeap s.locals.pq).capacity))
            (
              .seq (.basic (fun s => { s with locals := { s.locals with ret__val := 1 } })) .throw
            )
            .skip
        )
        (
          .seq
            (
              .guard (fun s => heapPtrValid s.globals.rawHeap (Ptr.elemOffset s.locals.data (hVal s.globals.rawHeap s.locals.pq).size.toNat))
                (
                  .basic (fun s => { s with globals := { s.globals with rawHeap := heapUpdate s.globals.rawHeap (Ptr.elemOffset s.locals.data (hVal s.globals.rawHeap s.locals.pq).size.toNat) s.locals.value } })
                )
            )
            (
              .seq
                (
                  .dynCom (fun saved =>
                    .seq
                      (.basic (fun s => { s with locals := { s.locals with data := s.locals.data, i := (hVal s.globals.rawHeap s.locals.pq).size } }))
                      (.seq
                        (.call "pq_bubble_up")
                        (.basic (fun s => { s with locals := saved.locals }))))
                )
                (
                  .seq
                    (
                      .guard (fun s => heapPtrValid s.globals.rawHeap s.locals.pq)
                        (
                          .guard (fun s => heapPtrValid s.globals.rawHeap s.locals.pq)
                            (
                              .basic (fun s => { s with globals := { s.globals with rawHeap := heapUpdate s.globals.rawHeap s.locals.pq ({ hVal s.globals.rawHeap s.locals.pq with size := ((hVal s.globals.rawHeap s.locals.pq).size + 1) }) } })
                            )
                        )
                    )
                    (
                      .seq (.basic (fun s => { s with locals := { s.locals with ret__val := 0 } })) .throw
                    )
                )
            )
        )
    )
    .skip

/-- CSimpl body for pq_extract_min(). -/
def pq_extract_min_body : CSimpl ProgramState :=
  .catch
    (
      .seq
        (
          .basic (fun s => { s with locals := { s.locals with root_idx := 0 } })
        )
        (
          .seq
            (
              .cond (fun s => decide ((hVal s.globals.rawHeap s.locals.pq).size = 0))
                (
                  .seq (.basic (fun s => { s with locals := { s.locals with ret__val := 1 } })) .throw
                )
                .skip
            )
            (
              .seq
                (
                  .guard (fun s => heapPtrValid s.globals.rawHeap s.locals.out)
                    (
                      .guard (fun s => heapPtrValid s.globals.rawHeap (Ptr.elemOffset s.locals.data s.locals.root_idx.toNat))
                        (
                          .basic (fun s => { s with globals := { s.globals with rawHeap := heapUpdate s.globals.rawHeap s.locals.out (hVal s.globals.rawHeap (Ptr.elemOffset s.locals.data s.locals.root_idx.toNat)) } })
                        )
                    )
                )
                (
                  .seq
                    (
                      .guard (fun s => heapPtrValid s.globals.rawHeap s.locals.pq)
                        (
                          .guard (fun s => heapPtrValid s.globals.rawHeap s.locals.pq)
                            (
                              .basic (fun s => { s with globals := { s.globals with rawHeap := heapUpdate s.globals.rawHeap s.locals.pq ({ hVal s.globals.rawHeap s.locals.pq with size := ((hVal s.globals.rawHeap s.locals.pq).size - 1) }) } })
                            )
                        )
                    )
                    (
                      .seq
                        (
                          .cond (fun s => decide ((hVal s.globals.rawHeap s.locals.pq).size > 0))
                            (
                              .seq
                                (
                                  .guard (fun s => heapPtrValid s.globals.rawHeap (Ptr.elemOffset s.locals.data s.locals.root_idx.toNat))
                                    (
                                      .guard (fun s => heapPtrValid s.globals.rawHeap (Ptr.elemOffset s.locals.data (hVal s.globals.rawHeap s.locals.pq).size.toNat))
                                        (
                                          .guard (fun s => heapPtrValid s.globals.rawHeap s.locals.pq)
                                            (
                                              .basic (fun s => { s with globals := { s.globals with rawHeap := heapUpdate s.globals.rawHeap (Ptr.elemOffset s.locals.data s.locals.root_idx.toNat) (hVal s.globals.rawHeap (Ptr.elemOffset s.locals.data (hVal s.globals.rawHeap s.locals.pq).size.toNat)) } })
                                            )
                                        )
                                    )
                                )
                                (
                                  .dynCom (fun saved =>
                                    .seq
                                      (.basic (fun s => { s with locals := { s.locals with data := s.locals.data, heap_size := (hVal s.globals.rawHeap s.locals.pq).size, i := s.locals.root_idx } }))
                                      (.seq
                                        (.call "pq_bubble_down")
                                        (.basic (fun s => { s with locals := saved.locals }))))
                                )
                            )
                            .skip
                        )
                        (
                          .seq (.basic (fun s => { s with locals := { s.locals with ret__val := 0 } })) .throw
                        )
                    )
                )
            )
        )
    )
    .skip

/-- CSimpl body for pq_peek(). -/
def pq_peek_body : CSimpl ProgramState :=
  .catch
    (
      .seq
        (
          .basic (fun s => { s with locals := { s.locals with root_idx := 0 } })
        )
        (
          .seq
            (
              .cond (fun s => decide ((hVal s.globals.rawHeap s.locals.pq).size = 0))
                (
                  .seq (.basic (fun s => { s with locals := { s.locals with ret__val := 1 } })) .throw
                )
                .skip
            )
            (
              .seq
                (
                  .guard (fun s => heapPtrValid s.globals.rawHeap s.locals.out)
                    (
                      .guard (fun s => heapPtrValid s.globals.rawHeap (Ptr.elemOffset s.locals.data s.locals.root_idx.toNat))
                        (
                          .basic (fun s => { s with globals := { s.globals with rawHeap := heapUpdate s.globals.rawHeap s.locals.out (hVal s.globals.rawHeap (Ptr.elemOffset s.locals.data s.locals.root_idx.toNat)) } })
                        )
                    )
                )
                (
                  .seq (.basic (fun s => { s with locals := { s.locals with ret__val := 0 } })) .throw
                )
            )
        )
    )
    .skip

/-- CSimpl body for pq_size(). -/
def pq_size_body : CSimpl ProgramState :=
  .catch
    (
      .guard (fun s => heapPtrValid s.globals.rawHeap s.locals.pq)
        (
          .seq (.basic (fun s => { s with locals := { s.locals with ret__val := (hVal s.globals.rawHeap s.locals.pq).size } })) .throw
        )
    )
    .skip

/-- CSimpl body for pq_is_empty(). -/
def pq_is_empty_body : CSimpl ProgramState :=
  .catch
    (
      .guard (fun s => heapPtrValid s.globals.rawHeap s.locals.pq)
        (
          .seq (.basic (fun s => { s with locals := { s.locals with ret__val := (if (hVal s.globals.rawHeap s.locals.pq).size == 0 then 1 else 0) } })) .throw
        )
    )
    .skip

/-- CSimpl body for pq_is_full(). -/
def pq_is_full_body : CSimpl ProgramState :=
  .catch
    (
      .guard (fun s => heapPtrValid s.globals.rawHeap s.locals.pq)
        (
          .guard (fun s => heapPtrValid s.globals.rawHeap s.locals.pq)
            (
              .seq (.basic (fun s => { s with locals := { s.locals with ret__val := (if (hVal s.globals.rawHeap s.locals.pq).size >= (hVal s.globals.rawHeap s.locals.pq).capacity then 1 else 0) } })) .throw
            )
        )
    )
    .skip

/-- Procedure environment mapping function names to bodies. -/
def procEnv : ProcEnv ProgramState :=
  ProcEnv.insert (ProcEnv.insert (ProcEnv.insert (ProcEnv.insert (ProcEnv.insert (ProcEnv.insert (ProcEnv.insert (ProcEnv.insert (ProcEnv.insert (ProcEnv.insert (ProcEnv.empty) "pq_init" pq_init_body) "pq_swap" pq_swap_body) "pq_bubble_up" pq_bubble_up_body) "pq_bubble_down" pq_bubble_down_body) "pq_insert" pq_insert_body) "pq_extract_min" pq_extract_min_body) "pq_peek" pq_peek_body) "pq_size" pq_size_body) "pq_is_empty" pq_is_empty_body) "pq_is_full" pq_is_full_body

end PriorityQueue
