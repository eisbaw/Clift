-- Generated by CImporter from clang JSON AST. Do not edit.
-- Source module: StateMachine
-- Regenerate with: just import test/c_sources/statemachine.c StateMachine

import Clift.CSemantics

set_option maxHeartbeats 400000
set_option linter.unusedVariables false

namespace StateMachine

/-- C struct conn_state: size=16, align=4 -/
structure C_conn_state where
  state : UInt32
  tx_bytes : UInt32
  rx_bytes : UInt32
  transition_count : UInt32
  deriving Repr, Inhabited

instance : CType C_conn_state where
  size := 16
  align := 4
  typeTag := ⟨200⟩
  size_pos := by omega

/-- Read C_conn_state from 16 bytes. -/
def C_conn_state.fromBytes (f : Fin 16 → UInt8) : C_conn_state where
  state := UInt32.fromBytes' (fun i => f ⟨0 + i.val, by omega⟩)
  tx_bytes := UInt32.fromBytes' (fun i => f ⟨4 + i.val, by omega⟩)
  rx_bytes := UInt32.fromBytes' (fun i => f ⟨8 + i.val, by omega⟩)
  transition_count := UInt32.fromBytes' (fun i => f ⟨12 + i.val, by omega⟩)

/-- Write C_conn_state to 16 bytes. -/
def C_conn_state.toBytes (v : C_conn_state) : Fin 16 → UInt8 := fun i =>
  if h : 0 ≤ i.val ∧ i.val < 4 then
    UInt32.toBytes' v.state ⟨i.val - 0, by omega⟩
  else
    if h : 4 ≤ i.val ∧ i.val < 8 then
      UInt32.toBytes' v.tx_bytes ⟨i.val - 4, by omega⟩
    else
      if h : 8 ≤ i.val ∧ i.val < 12 then
        UInt32.toBytes' v.rx_bytes ⟨i.val - 8, by omega⟩
      else
        if h : 12 ≤ i.val ∧ i.val < 16 then
          UInt32.toBytes' v.transition_count ⟨i.val - 12, by omega⟩
        else
          0  -- padding bytes

private theorem C_conn_state.toBytes_state_byte (v : C_conn_state) (i : Nat) (hi : i < 4) :
  C_conn_state.toBytes v ⟨0 + i, by omega⟩ = UInt32.toBytes' v.state ⟨i, hi⟩ := by
    unfold C_conn_state.toBytes
    rw [dif_pos (show (0 ≤ (0 + i) ∧ (0 + i) < 4) from by omega)]
    congr 1; apply Fin.ext; simp

private theorem C_conn_state.toBytes_tx_bytes_byte (v : C_conn_state) (i : Nat) (hi : i < 4) :
  C_conn_state.toBytes v ⟨4 + i, by omega⟩ = UInt32.toBytes' v.tx_bytes ⟨i, hi⟩ := by
    unfold C_conn_state.toBytes
    rw [dif_neg (show ¬(0 ≤ (4 + i) ∧ (4 + i) < 4) from by omega)]
    rw [dif_pos (show (4 ≤ (4 + i) ∧ (4 + i) < 8) from by omega)]
    congr 1; apply Fin.ext; simp

private theorem C_conn_state.toBytes_rx_bytes_byte (v : C_conn_state) (i : Nat) (hi : i < 4) :
  C_conn_state.toBytes v ⟨8 + i, by omega⟩ = UInt32.toBytes' v.rx_bytes ⟨i, hi⟩ := by
    unfold C_conn_state.toBytes
    rw [dif_neg (show ¬(0 ≤ (8 + i) ∧ (8 + i) < 4) from by omega)]
    rw [dif_neg (show ¬(4 ≤ (8 + i) ∧ (8 + i) < 8) from by omega)]
    rw [dif_pos (show (8 ≤ (8 + i) ∧ (8 + i) < 12) from by omega)]
    congr 1; apply Fin.ext; simp

private theorem C_conn_state.toBytes_transition_count_byte (v : C_conn_state) (i : Nat) (hi : i < 4) :
  C_conn_state.toBytes v ⟨12 + i, by omega⟩ = UInt32.toBytes' v.transition_count ⟨i, hi⟩ := by
    unfold C_conn_state.toBytes
    rw [dif_neg (show ¬(0 ≤ (12 + i) ∧ (12 + i) < 4) from by omega)]
    rw [dif_neg (show ¬(4 ≤ (12 + i) ∧ (12 + i) < 8) from by omega)]
    rw [dif_neg (show ¬(8 ≤ (12 + i) ∧ (12 + i) < 12) from by omega)]
    rw [dif_pos (show (12 ≤ (12 + i) ∧ (12 + i) < 16) from by omega)]
    congr 1; apply Fin.ext; simp

/-- Roundtrip: fromBytes (toBytes v) = v. -/
theorem C_conn_state.fromBytes_toBytes (v : C_conn_state) :
  C_conn_state.fromBytes (C_conn_state.toBytes v) = v := by
    have h_state : (fun i : Fin 4 => C_conn_state.toBytes v ⟨0 + i.val, by omega⟩) = UInt32.toBytes' v.state := by
      funext ⟨i, hi⟩; exact C_conn_state.toBytes_state_byte v i hi
    have h_tx_bytes : (fun i : Fin 4 => C_conn_state.toBytes v ⟨4 + i.val, by omega⟩) = UInt32.toBytes' v.tx_bytes := by
      funext ⟨i, hi⟩; exact C_conn_state.toBytes_tx_bytes_byte v i hi
    have h_rx_bytes : (fun i : Fin 4 => C_conn_state.toBytes v ⟨8 + i.val, by omega⟩) = UInt32.toBytes' v.rx_bytes := by
      funext ⟨i, hi⟩; exact C_conn_state.toBytes_rx_bytes_byte v i hi
    have h_transition_count : (fun i : Fin 4 => C_conn_state.toBytes v ⟨12 + i.val, by omega⟩) = UInt32.toBytes' v.transition_count := by
      funext ⟨i, hi⟩; exact C_conn_state.toBytes_transition_count_byte v i hi
    show C_conn_state.fromBytes (C_conn_state.toBytes v) = v
    unfold C_conn_state.fromBytes
    rw [h_state, h_tx_bytes, h_rx_bytes, h_transition_count, UInt32.fromBytes_toBytes' v.state, UInt32.fromBytes_toBytes' v.tx_bytes, UInt32.fromBytes_toBytes' v.rx_bytes, UInt32.fromBytes_toBytes' v.transition_count]
    try { cases v; rfl }

instance : MemType C_conn_state where
  size := 16
  align := 4
  typeTag := ⟨200⟩
  size_pos := by omega
  fromMem := C_conn_state.fromBytes
  toMem := C_conn_state.toBytes
  roundtrip := C_conn_state.fromBytes_toBytes

/-- Local variables (merged from all functions).
    NOTE: Locals use Inhabited default (zero-init). See task-0060. -/
structure Locals where
  bytes : UInt32
  conn : Ptr C_conn_state
  current : UInt32
  event : UInt32
  next : UInt32
  ret__val : UInt32
  state : UInt32
  deriving Repr, DecidableEq, Inhabited

/-- Program state combining globals and locals. -/
abbrev ProgramState := CState Locals

/-- CSimpl body for conn_init(). -/
def conn_init_body : CSimpl ProgramState :=
  .catch
    (
      .seq
        (
          .guard (fun s => heapPtrValid s.globals.rawHeap s.locals.conn)
            (
              .basic (fun s => { s with globals := { s.globals with rawHeap := heapUpdate s.globals.rawHeap s.locals.conn ({ hVal s.globals.rawHeap s.locals.conn with state := 0 }) } })
            )
        )
        (
          .seq
            (
              .guard (fun s => heapPtrValid s.globals.rawHeap s.locals.conn)
                (
                  .basic (fun s => { s with globals := { s.globals with rawHeap := heapUpdate s.globals.rawHeap s.locals.conn ({ hVal s.globals.rawHeap s.locals.conn with tx_bytes := 0 }) } })
                )
            )
            (
              .seq
                (
                  .guard (fun s => heapPtrValid s.globals.rawHeap s.locals.conn)
                    (
                      .basic (fun s => { s with globals := { s.globals with rawHeap := heapUpdate s.globals.rawHeap s.locals.conn ({ hVal s.globals.rawHeap s.locals.conn with rx_bytes := 0 }) } })
                    )
                )
                (
                  .guard (fun s => heapPtrValid s.globals.rawHeap s.locals.conn)
                    (
                      .basic (fun s => { s with globals := { s.globals with rawHeap := heapUpdate s.globals.rawHeap s.locals.conn ({ hVal s.globals.rawHeap s.locals.conn with transition_count := 0 }) } })
                    )
                )
            )
        )
    )
    .skip

/-- CSimpl body for conn_next_state(). -/
def conn_next_state_body : CSimpl ProgramState :=
  .catch
    (
      .seq
        (
          .cond (fun s => (decide (s.locals.current = 0) && decide (s.locals.event = 0)))
            (
              .seq (.basic (fun s => { s with locals := { s.locals with ret__val := 1 } })) .throw
            )
            .skip
        )
        (
          .seq
            (
              .cond (fun s => (decide (s.locals.current = 0) && decide (s.locals.event = 1)))
                (
                  .seq (.basic (fun s => { s with locals := { s.locals with ret__val := 2 } })) .throw
                )
                .skip
            )
            (
              .seq
                (
                  .cond (fun s => (decide (s.locals.current = 1) && decide (s.locals.event = 2)))
                    (
                      .seq (.basic (fun s => { s with locals := { s.locals with ret__val := 3 } })) .throw
                    )
                    .skip
                )
                (
                  .seq
                    (
                      .cond (fun s => (decide (s.locals.current = 1) && decide (s.locals.event = 5)))
                        (
                          .seq (.basic (fun s => { s with locals := { s.locals with ret__val := 0 } })) .throw
                        )
                        .skip
                    )
                    (
                      .seq
                        (
                          .cond (fun s => (decide (s.locals.current = 2) && decide (s.locals.event = 2)))
                            (
                              .seq (.basic (fun s => { s with locals := { s.locals with ret__val := 3 } })) .throw
                            )
                            .skip
                        )
                        (
                          .seq
                            (
                              .cond (fun s => (decide (s.locals.current = 3) && decide (s.locals.event = 3)))
                                (
                                  .seq (.basic (fun s => { s with locals := { s.locals with ret__val := 4 } })) .throw
                                )
                                .skip
                            )
                            (
                              .seq
                                (
                                  .cond (fun s => (decide (s.locals.current = 3) && decide (s.locals.event = 5)))
                                    (
                                      .seq (.basic (fun s => { s with locals := { s.locals with ret__val := 0 } })) .throw
                                    )
                                    .skip
                                )
                                (
                                  .seq
                                    (
                                      .cond (fun s => (decide (s.locals.current = 4) && decide (s.locals.event = 4)))
                                        (
                                          .seq (.basic (fun s => { s with locals := { s.locals with ret__val := 4 } })) .throw
                                        )
                                        .skip
                                    )
                                    (
                                      .seq
                                        (
                                          .cond (fun s => (decide (s.locals.current = 4) && decide (s.locals.event = 5)))
                                            (
                                              .seq (.basic (fun s => { s with locals := { s.locals with ret__val := 5 } })) .throw
                                            )
                                            .skip
                                        )
                                        (
                                          .seq
                                            (
                                              .cond (fun s => (decide (s.locals.current = 5) && decide (s.locals.event = 5)))
                                                (
                                                  .seq (.basic (fun s => { s with locals := { s.locals with ret__val := 0 } })) .throw
                                                )
                                                .skip
                                            )
                                            (
                                              .seq (.basic (fun s => { s with locals := { s.locals with ret__val := 255 } })) .throw
                                            )
                                        )
                                    )
                                )
                            )
                        )
                    )
                )
            )
        )
    )
    .skip

/-- CSimpl body for conn_transition(). -/
def conn_transition_body : CSimpl ProgramState :=
  .catch
    (
      .seq
        (
          .dynCom (fun saved =>
            .seq
              (.basic (fun s => { s with locals := { s.locals with current := (hVal s.globals.rawHeap s.locals.conn).state, event := s.locals.event } }))
              (.seq
                (.call "conn_next_state")
                (.basic (fun s => { s with locals := { saved.locals with next := s.locals.ret__val } }))))
        )
        (
          .seq
            (
              .cond (fun s => decide (s.locals.next = 255))
                (
                  .seq (.basic (fun s => { s with locals := { s.locals with ret__val := 1 } })) .throw
                )
                .skip
            )
            (
              .seq
                (
                  .guard (fun s => heapPtrValid s.globals.rawHeap s.locals.conn)
                    (
                      .basic (fun s => { s with globals := { s.globals with rawHeap := heapUpdate s.globals.rawHeap s.locals.conn ({ hVal s.globals.rawHeap s.locals.conn with state := s.locals.next }) } })
                    )
                )
                (
                  .seq
                    (
                      .guard (fun s => heapPtrValid s.globals.rawHeap s.locals.conn)
                        (
                          .guard (fun s => heapPtrValid s.globals.rawHeap s.locals.conn)
                            (
                              .basic (fun s => { s with globals := { s.globals with rawHeap := heapUpdate s.globals.rawHeap s.locals.conn ({ hVal s.globals.rawHeap s.locals.conn with transition_count := ((hVal s.globals.rawHeap s.locals.conn).transition_count + 1) }) } })
                            )
                        )
                    )
                    (
                      .seq (.basic (fun s => { s with locals := { s.locals with ret__val := 0 } })) .throw
                    )
                )
            )
        )
    )
    .skip

/-- CSimpl body for conn_send(). -/
def conn_send_body : CSimpl ProgramState :=
  .catch
    (
      .seq
        (
          .cond (fun s => decide ((hVal s.globals.rawHeap s.locals.conn).state ≠ 4))
            (
              .seq (.basic (fun s => { s with locals := { s.locals with ret__val := 1 } })) .throw
            )
            .skip
        )
        (
          .seq
            (
              .guard (fun s => heapPtrValid s.globals.rawHeap s.locals.conn)
                (
                  .guard (fun s => heapPtrValid s.globals.rawHeap s.locals.conn)
                    (
                      .basic (fun s => { s with globals := { s.globals with rawHeap := heapUpdate s.globals.rawHeap s.locals.conn ({ hVal s.globals.rawHeap s.locals.conn with tx_bytes := ((hVal s.globals.rawHeap s.locals.conn).tx_bytes + s.locals.bytes) }) } })
                    )
                )
            )
            (
              .seq (.basic (fun s => { s with locals := { s.locals with ret__val := 0 } })) .throw
            )
        )
    )
    .skip

/-- CSimpl body for conn_recv(). -/
def conn_recv_body : CSimpl ProgramState :=
  .catch
    (
      .seq
        (
          .cond (fun s => decide ((hVal s.globals.rawHeap s.locals.conn).state ≠ 4))
            (
              .seq (.basic (fun s => { s with locals := { s.locals with ret__val := 1 } })) .throw
            )
            .skip
        )
        (
          .seq
            (
              .guard (fun s => heapPtrValid s.globals.rawHeap s.locals.conn)
                (
                  .guard (fun s => heapPtrValid s.globals.rawHeap s.locals.conn)
                    (
                      .basic (fun s => { s with globals := { s.globals with rawHeap := heapUpdate s.globals.rawHeap s.locals.conn ({ hVal s.globals.rawHeap s.locals.conn with rx_bytes := ((hVal s.globals.rawHeap s.locals.conn).rx_bytes + s.locals.bytes) }) } })
                    )
                )
            )
            (
              .seq (.basic (fun s => { s with locals := { s.locals with ret__val := 0 } })) .throw
            )
        )
    )
    .skip

/-- CSimpl body for conn_get_state(). -/
def conn_get_state_body : CSimpl ProgramState :=
  .catch
    (
      .guard (fun s => heapPtrValid s.globals.rawHeap s.locals.conn)
        (
          .seq (.basic (fun s => { s with locals := { s.locals with ret__val := (hVal s.globals.rawHeap s.locals.conn).state } })) .throw
        )
    )
    .skip

/-- CSimpl body for conn_is_established(). -/
def conn_is_established_body : CSimpl ProgramState :=
  .catch
    (
      .guard (fun s => heapPtrValid s.globals.rawHeap s.locals.conn)
        (
          .seq (.basic (fun s => { s with locals := { s.locals with ret__val := (if (hVal s.globals.rawHeap s.locals.conn).state == 4 then 1 else 0) } })) .throw
        )
    )
    .skip

/-- CSimpl body for conn_state_valid(). -/
def conn_state_valid_body : CSimpl ProgramState :=
  .catch
    (
      .seq (.basic (fun s => { s with locals := { s.locals with ret__val := (if s.locals.state < 6 then 1 else 0) } })) .throw
    )
    .skip

/-- CSimpl body for conn_get_transitions(). -/
def conn_get_transitions_body : CSimpl ProgramState :=
  .catch
    (
      .guard (fun s => heapPtrValid s.globals.rawHeap s.locals.conn)
        (
          .seq (.basic (fun s => { s with locals := { s.locals with ret__val := (hVal s.globals.rawHeap s.locals.conn).transition_count } })) .throw
        )
    )
    .skip

/-- Procedure environment mapping function names to bodies. -/
def procEnv : ProcEnv ProgramState :=
  ProcEnv.insert (ProcEnv.insert (ProcEnv.insert (ProcEnv.insert (ProcEnv.insert (ProcEnv.insert (ProcEnv.insert (ProcEnv.insert (ProcEnv.insert (ProcEnv.empty) "conn_init" conn_init_body) "conn_next_state" conn_next_state_body) "conn_transition" conn_transition_body) "conn_send" conn_send_body) "conn_recv" conn_recv_body) "conn_get_state" conn_get_state_body) "conn_is_established" conn_is_established_body) "conn_state_valid" conn_state_valid_body) "conn_get_transitions" conn_get_transitions_body

end StateMachine
