-- Generated by CImporter from clang JSON AST. Do not edit.
-- Source module: Bitwise
-- Regenerate with: just import test/c_sources/bitwise.c Bitwise

import Clift.CSemantics

set_option maxHeartbeats 400000
set_option linter.unusedVariables false

namespace Bitwise

/-- Local variables (merged from all functions).
    NOTE: Locals use Inhabited default (zero-init). See task-0060. -/
structure Locals where
  hi : UInt32
  hi_half : UInt32
  lo : UInt32
  lo_half : UInt32
  mask : UInt32
  n : UInt32
  ret__val : UInt32
  val : UInt32
  deriving Repr, DecidableEq, Inhabited

/-- Program state combining globals and locals. -/
abbrev ProgramState := CState Locals

/-- CSimpl body for extract_bits(). -/
def extract_bits_body : CSimpl ProgramState :=
  .catch
    (
      .seq
        (
          .basic (fun s => { s with locals := { s.locals with mask := (((1 <<< ((s.locals.hi - s.locals.lo) + 1)) - 1) <<< s.locals.lo) } })
        )
        (
          .seq (.basic (fun s => { s with locals := { s.locals with ret__val := ((s.locals.val &&& s.locals.mask) >>> s.locals.lo) } })) .throw
        )
    )
    .skip

/-- CSimpl body for set_bit(). -/
def set_bit_body : CSimpl ProgramState :=
  .catch
    (
      .seq (.basic (fun s => { s with locals := { s.locals with ret__val := (s.locals.val ||| (1 <<< s.locals.n)) } })) .throw
    )
    .skip

/-- CSimpl body for clear_bit(). -/
def clear_bit_body : CSimpl ProgramState :=
  .catch
    (
      .seq (.basic (fun s => { s with locals := { s.locals with ret__val := (s.locals.val &&& (~~~(1 <<< s.locals.n))) } })) .throw
    )
    .skip

/-- CSimpl body for toggle_bit(). -/
def toggle_bit_body : CSimpl ProgramState :=
  .catch
    (
      .seq (.basic (fun s => { s with locals := { s.locals with ret__val := (s.locals.val ^^^ (1 <<< s.locals.n)) } })) .throw
    )
    .skip

/-- CSimpl body for combine_halves(). -/
def combine_halves_body : CSimpl ProgramState :=
  .catch
    (
      .seq (.basic (fun s => { s with locals := { s.locals with ret__val := ((s.locals.hi_half <<< 16) ||| s.locals.lo_half) } })) .throw
    )
    .skip

/-- Procedure environment mapping function names to bodies. -/
def procEnv : ProcEnv ProgramState :=
  ProcEnv.insert (ProcEnv.insert (ProcEnv.insert (ProcEnv.insert (ProcEnv.insert (ProcEnv.empty) "extract_bits" extract_bits_body) "set_bit" set_bit_body) "clear_bit" clear_bit_body) "toggle_bit" toggle_bit_body) "combine_halves" combine_halves_body

end Bitwise
